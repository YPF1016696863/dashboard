To deploy the server run:
docker-compose up

----
Manual steps after (in another shell)
Run all scripts in order in /scripts folder
1. setup the db schema
2. setup the default user
3. setup the demo db
docker run --name=datavis_demo_mysql -v /root/mysql_data:/var/lib/mysql -e MYSQL_ROOT_PASSWORD=rukshani -p 3306:3306 -d --network=demo --network-alias mysql mysql:5.6

if already existed:
docker network connect datavisserver_default datavis_demo_mysql

----
Other APIs:
----
Login

curl -i -X POST \
   -H "Content-Type:application/json" \
   -d \
'{
  "email":"test@gmail.com",
  "password":"fcfcfc"
}' \
 'http://localhost:5000/api/login'

Logout
curl -i -X POST \
   -H "Content-Type:application/json" \
   -d \
'{
}' \
 'http://localhost:5000/api/logout'

----
Configure data source

server: datavis_demo_mysql:3306
user: cboard
pass: cboard
db: foodmart2
